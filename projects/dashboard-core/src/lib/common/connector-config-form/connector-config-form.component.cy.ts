import { ConnectorConfigFormComponent } from './connector-config-form.component';

describe('ConnectorConfigFormComponent', () => {
  it('should mount', () => {
    cy.mount(ConnectorConfigFormComponent);
  });
});
