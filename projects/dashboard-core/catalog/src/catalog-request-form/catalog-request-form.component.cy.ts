import { CatalogRequestFormComponent } from './catalog-request-form.component';

describe('CatalogRequestFormComponent', () => {
  it('should mount', () => {
    cy.mount(CatalogRequestFormComponent);
  });
});
